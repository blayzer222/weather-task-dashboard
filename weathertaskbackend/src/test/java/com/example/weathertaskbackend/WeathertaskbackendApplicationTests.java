package com.example.weathertaskbackend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/*
 * Dies ist eine automatisch generierte Testklasse,
 * die Spring Boot für jedes neue Projekt erstellt.
 *
 * Zweck:
 * - Sie testet, ob der Spring Application Context korrekt geladen wird.
 * - Das bedeutet: Kann die Anwendung grundsätzlich starten?
 *
 * Wenn dieser Test fehlschlägt, ist meistens ein grundlegender Fehler in:
 * - Beans
 * - Konfigurationen
 * - Fehlen von Abhängigkeiten
 * - fehlerhaften Annotationen
 *
 * Für kleine Projekte reicht dieser Test aus,
 * bei Produktionssystemen würde man weitere Unit- und Integrationstests hinzufügen.
 */
@SpringBootTest
class WeathertaskbackendApplicationTests {

    /*
     * Der einfachste mögliche Test:
     * Prüft nur, ob der Spring-Kontext ohne Fehler gestartet werden kann.
     *
     * Wenn die Anwendung fehlerfrei startet,
     * gilt der Test als erfolgreich — auch ohne Assertions.
     */
    @Test
    void contextLoads() {
        // kein Inhalt notwendig — Start der App reicht als Testkriterium
    }

}
